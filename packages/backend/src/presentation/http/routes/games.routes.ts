import { Hono } from 'hono';

const gamesRoutes = new Hono();

// Nota: Funcionalidades de jogos foram removidas para garantir conformidade regulatória.
// O sistema agora foca exclusivamente em recompensas por engajamento e marketplace.

export { gamesRoutes };
